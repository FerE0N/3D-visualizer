/** @type {import('next').NextConfig} */
const nextConfig = {
  allowedDevOrigins: ['visor-eon.loca.lt'],
  // Se ha removido la configuración de exportación estática para compatibilidad con servidores Node como Render
};

export default nextConfig;
